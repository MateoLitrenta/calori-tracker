const fs = require('fs'); let f = fs.readFileSync('src/components/DailyPanel.tsx', 'utf8'); f = f.replace(/className=\"([^\"]*)text-sm([^\"]*)\"/g, (match, p1, p2) => { if (p1.includes('w-16') || p1.includes('bg-github-bg') || p1.includes('min-w') || p1.includes('w-24') || p1.includes('hover:border')) return \className=\\\"\text-base\\\\"\; return match; }); fs.writeFileSync('src/components/DailyPanel.tsx', f);